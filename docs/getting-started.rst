Getting started with pyvisdk
========================================

This will be uploaded to the Python Package Index when it becomes 
more stable, but for now you can download the code from github, 
then run: 

  $ sudo python ./setup.py install
